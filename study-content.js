(function(root){
 'use strict';
 const C=NNCore,V=NNVisual,T=String.raw,f=C.fmt,v=C.vec;
 const table=(heads,rows)=>`<div class="table-wrap"><table><thead><tr>${heads.map(x=>`<th>${x}</th>`).join('')}</tr></thead><tbody>${rows.map(r=>`<tr>${r.map(x=>`<td>${x}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;
 const detail=(title,body)=>`<details class="calculation"><summary>${title}</summary>${body}</details>`;
 const field=(id,label,answer,extra={})=>({id,label,answer,...extra});
 const recipes={
  tlu:{title:'TLU: Rand → Seite → Netz',source:'Folien 17–35, 80',steps:[T`Lies zwei Punkte je Randgerade ab. Vertikal: $x_1=c$. Horizontal: $x_2=c$.`,T`Für eine schräge Kante durch $A,B$: $w=(B_2-A_2,A_1-B_1)$ und $\theta=w\cdot A$.`,T`Setze einen Punkt der gewünschten Seite ein. Falls $w\cdot P<\theta$, multipliziere $w$ und $\theta$ gemeinsam mit $-1$.`,T`Schreibe auf, welche Tests zusammen gelten müssen. Finde eine Ausgabe-TLU für diese logische Bedingung. Prüfe deren Wahrheitstabelle.`,T`Zähle die Eingabeschicht mit. Gib alle Gewichte, Schwellen und Verbindungen an. Prüfe Innenpunkte und jede ausgesparte Seite.`],why:T`Eine TLU gibt $1$ aus, wenn $w\cdot x\ge\theta$. Die Randgerade allein legt die richtige Seite noch nicht fest. Positive gemeinsame Skalierung von $w$ und $\theta$ ändert den Test nicht. Ein zusätzliches AND- und OR-Netz hätte zusammen mit Eingabe und Randtests vier Schichten.`,example:T`Durch $(1,3)$ und $(3,1)$: $w=(-2,-2)$, $\theta=-8$. Für den inneren Punkt $(1,1)$ gilt $-4\ge-8$. Vereinfacht: $w=(-1,-1)$, $\theta=-4$, also $x_1+x_2\le4$.`},
  rbf:{title:'RBF-Fläche: Form → Zentrum → Ausgabe',source:'Folien 288–291',steps:[T`Erkenne die Bausteine: $L_1$-Ball = Raute, $L_2$-Ball = Kreis, $L_\infty$-Ball = achsenparalleles Quadrat.`,T`Lies Zentrum und Radius ab. Beim Quadrat ist der Radius die halbe Seitenlänge.`,T`Für einen scharf begrenzten Baustein: $h_i=1$ bei $d_i\le\sigma_i$, sonst $0$.`,T`Setze positive Gewichte für Grundflächen und negative für Abzüge. Der Ausgang der hier verwendeten RBF-Netze ist linear.`,T`Prüfe jeden räumlichen Fall: gewünschtes Inneres, ausgesparte Bereiche, außerhalb. Beachte, ob außen $0$ oder auch negative Werte erlaubt sind.`],why:T`$d_1(x,c)=|x_1-c_1|+|x_2-c_2|$, $d_\infty(x,c)=\max(|x_1-c_1|,|x_2-c_2|)$. Der Ausgang $y=\sum w_i h_i-\theta$ addiert die aktiven Bausteine. Bei überlappenden positiven Bausteinen muss die Summe kontrolliert werden.`,example:T`Zentrum $(2,2)$, Radius $1$: Eine $L_1$-Raute reicht bis $(1,2),(3,2),(2,1),(2,3)$. Ein $L_\infty$-Quadrat mit Radius $0.5$ hat Seitenlänge $1$.`},
  mlp:{title:'MLP-Näherung: Werte → Unterschiede → Stufen',source:'Folien 80, 99–104',steps:[T`Wähle Stützstellen $x_0,\ldots,x_k$ und berechne alle $y_i=f(x_i)$. Zähle die erlaubten Neuronen einschließlich Ein- und Ausgabe.`,T`Für jede Stelle ab $x_1$: $\Delta y_i=y_i-y_{i-1}$. Diese Unterschiede sind die Ausgabegewichte.`,T`Hidden-Neuron $i$: Eingangsgewicht $1$, Schwelle $x_i$, Aktivierung $h_i=\mathbf1_{x\ge x_i}$.`,T`Linearer Ausgang: $\widehat f(x)=y_0+\sum_i\Delta y_i h_i(x)$. Bei $out=net-\theta$ ist $\theta=-y_0$.`,T`Prüfe den ersten Abschnitt und mehrere Sprünge. Zeichne waagrechte Stücke und gib die Neuronenzahl an.`],why:T`Beim Überschreiten einer Schwelle bleibt jede frühere Stufe aktiv. Deshalb wird nur der Unterschied zur vorherigen Höhe addiert.`,example:T`Höhen $10,5,2$: Startwert $10$, Gewichte $-5,-3$. Nach dem ersten Sprung: $10-5=5$; nach dem zweiten: $10-5-3=2$.`},
  approx:{title:'RBF-Näherung: Zentren → Dreiecke → Höhen',source:'Folien 291–298',steps:[T`Wähle gleichmäßig verteilte Zentren mit Abstand $h$ und berechne $f(c_i)$.`,T`Verwende $d_i=|x-c_i|$, Radius $h$ und $h_i(x)=\max(0,1-d_i/h)$.`,T`Setze das Ausgabegewicht $w_i=f(c_i)$. Linearer Ausgang mit Schwelle $0$.`,T`Zwischen zwei Zentren sind genau zwei Dreiecke aktiv. Ihre Summe ist $1$; so entsteht lineare Interpolation.`,T`Prüfe ein Zentrum und einen Zwischenpunkt. Zähle alle Neuronen und zeichne die verbundenen Stützwerte.`],why:T`Bei Rechteckfenstern können sich an gemeinsamen Randpunkten zwei volle Höhen addieren. Dreiecke fallen am benachbarten Zentrum auf null. Die Konstruktion ist stetig.`,example:T`Zentren $0,1$, Höhen $2,6$: Bei $x=0.25$ sind die Aktivierungen $0.75$ und $0.25$. Ausgabe: $0.75\cdot2+0.25\cdot6=3$.`},
  lvq:{title:'LVQ: Abstand → Regel → neue Vektoren',source:'Folien 336, 340',steps:[T`Notiere Startvektoren, Klassen, Lernrate und Reihenfolge. Für getrennte Durchläufe jeweils wieder beim Start beginnen.`,T`Berechne die quadrierten Abstände zu den aktuellen Vektoren. Die kleinste Zahl bestimmt den Winner.`,T`Ohne Klassen: nur Winner anziehen. Gewinnerregel mit Klassen: passenden Winner anziehen, unpassenden abstoßen.`,T`Anziehen: $r'=r+\eta(x-r)$. Abstoßen: $r'=r-\eta(x-r)$. Nicht betroffene Vektoren bleiben gleich.`,T`Beim nächsten Datenpunkt die aktualisierten Vektoren verwenden. Am Ende zusätzlich die Gesamtänderung $r_{Ende}-r_{Start}$ angeben.`],why:T`Folie 340 beschreibt eine andere Regel: die zwei nächsten Prototypen prüfen; nur bei verschiedenen Klassen und passender Datenklasse beide verändern. Eine zusätzlich angegebene Window-Regel wäre ebenfalls zu beachten. Die Aufgabenübersicht von 2024 legt die Variante nicht eindeutig fest.`,example:T`$r=(4,2)$, $x=(5,2)$, $\eta=0.5$. Anziehen: $(4.5,2)$. Abstoßen: $(3.5,2)$. Bei Lernrate $0.5$: Anziehen bedeutet Mittelwert $(r+x)/2$.`},
  som:{title:'SOM: Winner → Gitterabstand → Update',source:'Folien 364–367',steps:[T`Finde den nächsten Prototyp im Datenraum. Notiere seinen Gitterindex.`,T`Berechne für jedes Neuron den Abstand im festen Neuronengitter: $d_G^2=(i-i_*)^2+(j-j_*)^2$.`,T`Berechne $h=e^{-d_G^2/(2\sigma^2)}$ und die effektive Lernrate $\alpha=\eta h$.`,T`Für jede Koordinate: $r^{neu}=r+\alpha(x-r)$. Alle Updates dieses Schritts verwenden das alte Gitter.`,T`Unterscheide Änderung $\Delta r$ und neuen Vektor. Gleiche Gitterabstände haben denselben Faktor; berechne ihn nur einmal.`],why:T`Der Winner wird im Datenraum gesucht. Die Nachbarschaft benutzt Gittereinheiten, auch wenn benachbarte Anfangsprototypen im Bild fünf Einheiten auseinanderliegen. Die Gaußfunktion ist bei endlichem Abstand positiv.`,example:T`$x=(28,20)$, Winner $(30,20)$, $\eta=0.5$: $h=1$ und $r^{neu}=(30,20)+0.5(-2,0)=(29,20)$. Ein direkter Nachbar hat bei $\sigma=2$ den Faktor $h=e^{-1/8}$.`},
  hopfield:{title:'Hopfield: Einzelupdates → Tabelle → Graph',source:'Folien 380–390',steps:[T`Schreibe die Zustandsreihenfolge, Gewichte und Schwellen auf. Hier: bipolar, symmetrische Gewichte und Diagonale null.`,T`Pro Neuron: $net_i=\sum_{j\ne i}w_{ij}s_j$. Vergleiche mit $\theta_i$: bei $net_i\ge\theta_i$ wird der neue Wert $+1$, sonst $-1$.`,T`Liste alle $2^n$ Ausgangszustände. Berechne je Zustand jedes Einzelupdate wieder vom selben Ausgangszustand aus.`,T`Übertrage jede Nachfolgerzelle als gerichteten Pfeil. Unveränderte Updates als Selbstschleife oder ausdrücklich als Liste angeben.`,T`Ein Zustand ist stabil, wenn alle Einzelupdates ihn erhalten. Bei drei Neuronen müssen $3\cdot8=24$ Einzelupdates erfasst sein.`],why:T`Asynchron bedeutet: höchstens ein Neuron ändert sich pro Übergang. Ein festes zyklisches Updateverfahren ist nur eine mögliche Bahn; ein vollständiger Graph enthält alle erlaubten Einzelupdates.`,example:T`Bei $net_1=-2s_2$, $\theta_1=-1$ und Ausgang $+--$ gilt $net_1=2\ge-1$. Update 1 lässt $+--$ unverändert. Die anderen zwei Zeichen dürfen dabei nicht geändert werden.`}
 };
 function recipeHTML(id){const r=recipes[id];return `<h3>${r.title}</h3><ol class="steps">${r.steps.map(s=>`<li>${s}</li>`).join('')}</ol><div class="example"><strong>Mit Zahlen</strong><p>${r.example}</p></div>${detail('Warum diese Schritte funktionieren',`<p>${r.why}</p>`)}<p class="source">${r.source}</p>`;}
 function tlu(kind){const original=kind==='original',points=original?[[1,4],[4,4],[3,1],[3,3]]:[[5,5],[2,5],[3,2],[3,4]],params=original?[[0,-1,-4],[-3,1,-8],[1,0,3],[1,2,9]]:[[0,-1,-5],[3,1,11],[-1,0,-3],[-1,2,5]],tests=original?[[2,3.8],[3.2,2],[2,2],[4,2],[2,5]]:[[4,4.8],[2.8,3],[4,3],[2,3],[4,6]],conditions=original?[T`x_2\le4`,T`-3x_1+x_2\ge-8`,T`x_1\ge3`,T`x_1+2x_2\ge9`]:[T`x_2\le5`,T`3x_1+x_2\ge11`,T`x_1\le3`,T`-x_1+2x_2\ge5`];
  const trace=p=>params.map(([a,b,t])=>a*p[0]+b*p[1]>=t?1:0),result=p=>{const hs=trace(p);return 2*hs[0]+2*hs[1]+hs[2]+hs[3]>=5?1:0;};
  const diagram=V.plane({polygons:[points],points:points.map(([x,y],i)=>({x,y,label:'ABCD'[i]})),range:[0,6,0,7],title:'Polygon mit beschrifteten Eckpunkten'});
  const solution=`<h3>1. Randtests bestimmen</h3><p>Die Punkte in Umlaufrichtung sind ${points.map((p,i)=>`${'ABCD'[i]}=${v(p)}`).join(', ')}. Die obere und die steile schräge Kante gelten für die gesamte Fläche. Zusätzlich muss eine der beiden übrigen Bedingungen gelten.</p>`+table(['Test','Kante','Bedingung','Gewicht w','Schwelle θ'],params.map((p,i)=>[`h${i+1}`,['AB','BC','CD','DA'][i],`$${conditions[i]}$`,v(p.slice(0,2)),p[2]]))+T`<p>Für BC: Richtungsvektor $C-B=${v(points[2].map((x,i)=>x-points[1][i]))}$. Ein senkrechter Vektor ist $w=${v(params[1].slice(0,2))}$. Mit B folgt $\theta=${params[1][0]}\cdot${points[1][0]}+${params[1][1]}\cdot${points[1][1]}=${params[1][2]}$. Ein innerer Testpunkt legt das Vorzeichen fest.</p><h3>2. Die Logik in eine Ausgabe-TLU übersetzen</h3>$$innen=h_1\land h_2\land(h_3\lor h_4).$$$$y=H(2h_1+2h_2+h_3+h_4-5).$$<p>Ausgabegewichte $(2,2,1,1)$, Schwelle $5$. Fällt $h_1$ oder $h_2$ aus, ist die Summe höchstens $4$. Sind beide erfüllt, ist die Summe $4$; mindestens einer der übrigen Tests muss noch $1$ beitragen.</p>`+V.network(['h₁','h₂','h₃','h₄'],[2,2,1,1])+T`<p><strong>Genau drei Schichten:</strong> zwei Eingabeneuronen → vier verdeckte TLUs → eine Ausgabe-TLU. Alle TLUs verwenden $H(t)=1$ bei $t\ge0$, sonst $0$; Ausgabefunktionen sind Identitäten. Alle Verbindungen sind in Diagramm und Tabelle angegeben; zusätzliche Verbindungen gibt es nicht.</p><h3>3. Fälle kontrollieren</h3>`+table(['Punkt','h₁,h₂,h₃,h₄','Gewichtete Summe','y'],tests.map(p=>[v(p),v(trace(p)),trace(p).reduce((s,x,i)=>s+x*[2,2,1,1][i],0),result(p)]))+`<p>Die Fallunterscheidung der logischen Bedingung begründet die ganze Fläche. Einzelne Testpunkte helfen zusätzlich, Vorzeichenfehler zu finden.</p>`;
  return {id:`${kind}-tlu`,family:'tlu',kind,title:'TLU: die Fläche als Netz',purpose:original?'Aufgabe 1 · Zahlen und Form der Vorlage':'Gespiegelte und verschobene Form; Seiten und Gewichte selbst bestimmen',source:'2024, Aufgabe 1 · Folien 17–35, 80',parts:[{id:'netz',title:'Netz konstruieren',recipes:['tlu'],prompt:T`Konstruiere ein dreischichtiges Perzeptron, das innerhalb der gezeigten Fläche $+1$ und außerhalb $0$ liefert. Gib alle Gewichte, Schwellen und Verbindungen an.`,clarification:T`Konvention nach Folie 80: Die Eingabeschicht zählt mit. Randpunkte zählen hier zur Fläche. ${original?'Die Koordinaten sind aus der Vorlage abgelesen.':'Eigene Transferaufgabe; die Zeichnung ist die Vorgabe.'}`,image:original?'exam_task_img_19.png':null,figure:diagram,hints:[T`Die vier Randkanten liefern vier Tests. Finde zuerst die beiden Bedingungen, die für jeden inneren Punkt gelten.`,T`Für diese Form gilt: innen = Test AB UND Test BC UND (Test CD ODER Test DA).`,T`Gib den beiden immer nötigen Tests Gewicht 2, den zwei Alternativen Gewicht 1. Welche Schwelle fordert die beiden Pflichtbedingungen und mindestens eine Alternative?`],fields:params.map((p,i)=>field(`h${i+1}`,`Kante ${['AB','BC','CD','DA'][i]}: (w₁; w₂; θ) für die Innenseite`,p,{vector:true,scale:true})),solution,checks:['Randgeraden und gewünschte Seiten stimmen.','Netz hat drei Schichten einschließlich Eingabe.','Ausgabegewichte und Schwelle erfüllen die ganze logische Bedingung.','Alle Parameter und Verbindungen sind angegeben.'],model:{points,params,tests}}]};
 }
 function rbf(kind){const o=kind==='original',c=o?[2,2]:[1,2],r=o?1:2,sgn=o?1:-1,centers=[c,[c[0]-sgn*r/2,c[1]+r/2],[c[0]+sgn*r/2,c[1]-r/2]],polys=[[c,[c[0]+sgn*r,c[1]],[c[0],c[1]+r]],[c,[c[0]-sgn*r,c[1]],[c[0],c[1]-r]]],raute=[[c[0]-r,c[1]],[c[0],c[1]+r],[c[0]+r,c[1]],[c[0],c[1]-r]],sq=q=>[[q[0]-r/2,q[1]-r/2],[q[0]+r/2,q[1]-r/2],[q[0]+r/2,q[1]+r/2],[q[0]-r/2,q[1]+r/2]],range=[c[0]-r-1,c[0]+r+1,c[1]-r-1,c[1]+r+1];
  const desired=[c[0]+sgn*r/4,c[1]+r/4],excluded=[c[0]-sgn*r/4,c[1]+r/4],outside=[c[0]+2*r,c[1]];
  const evalPoint=p=>centers.map((q,i)=>(i===0?Math.abs(p[0]-q[0])+Math.abs(p[1]-q[1]):Math.max(Math.abs(p[0]-q[0]),Math.abs(p[1]-q[1])))<=(i===0?r:r/2)?1:0);
  const solution=`<h3>1. Grundfläche und Abzüge</h3><p>Die Raute um ${v(c)} deckt vier Dreiecke ab. Zwei sind gewünscht. Die zwei anderen werden mit Quadraten abgezogen.</p>`+V.plane({polygons:[raute,sq(centers[1]),sq(centers[2])],range,title:'Grundraute und die beiden quadratischen Abzüge'})+table(['Hidden-Neuron','Zentrum','Metrik','Radius','Ausgabegewicht'],centers.map((q,i)=>[`h${i+1}`,v(q),i?'L∞':'L₁',i?r/2:r,i?-1:1]))+T`<h3>2. Alle Funktionen angeben</h3><p>Netzeingabe des Hidden-Neurons ist der Abstand zum Zentrum. Die Aktivierung lautet $h_i=1$ bei $d_i\le\sigma_i$, sonst $0$; die Ausgabefunktion ist die Identität.</p>$$d_1=|x_1-${c[0]}|+|x_2-${c[1]}|,$$$$d_2=\max(|x_1-${centers[1][0]}|,|x_2-${centers[1][1]}|),$$$$d_3=\max(|x_1-${centers[2][0]}|,|x_2-${centers[2][1]}|).$$$$y=h_1-h_2-h_3.$$<p>Ausgang: Gewichte $(1,-1,-1)$, Schwelle $0$, lineare Aktivierung und Identität als Ausgabefunktion. Jeder Hidden-Knoten erhält beide Eingabekoordinaten.</p>`+V.network(['h₁','h₂','h₃'],[1,-1,-1])+`<h3>3. Drei räumliche Fälle</h3>`+table(['Punkt','h₁,h₂,h₃','y'],[desired,excluded,outside].map(p=>{const h=evalPoint(p);return [v(p),v(h),h[0]-h[1]-h[2]];}))+`<p>Im gewünschten Inneren ist nur die Raute aktiv: y=1. In jedem unerwünschten Rautenteil ist zusätzlich genau das zugehörige Quadrat aktiv: y=0. Außerhalb der Raute gilt h₁=0; die Abzüge können die Ausgabe nur auf 0 oder darunter setzen. Damit ist die Bedingung überall erfüllt. Ränder sind laut Aufgabe egal.</p>`;
  return {id:`${kind}-rbf`,family:'rbf',kind,title:'RBF: eine Fläche zusammensetzen',purpose:o?'Aufgabe 2 · Form und Zahlen der Vorlage':'Andere Diagonale, anderer Mittelpunkt und Radius',source:'2024, Aufgabe 2 · Folien 288–291',parts:[{id:'flaeche',title:'Netz konstruieren',recipes:['rbf'],prompt:T`Konstruiere ein RBF-Netzwerk mit Ausgabe $1$ innerhalb der schattierten Fläche und $\le0$ außerhalb. Ränder sind egal. Die Distanzfunktionen dürfen je Knoten verschieden sein, müssen aber zur Minkowski-Familie gehören. Gib Zentren, Distanzen, Netzeingaben, Aktivierungen und Gewichte vollständig an.`,image:o?'exam_task_img_20.png':null,figure:V.plane({polygons:polys,range,title:'Die zwei gewünschten Dreiecke'}),clarification:o?'Zentren und Koordinaten werden aus der Abbildung abgelesen.':'Eigene Transferaufgabe. Wähle die Bausteine selbst.',hints:['Welche Normkugel enthält die beiden Dreiecke und zwei weitere gleich große Dreiecke?','Ergänze die Zielfläche zu einer Raute. Ziehe die falschen Teile mit zwei achsenparallelen Quadraten ab.',T`Die Raute hat Zentrum ${v(c)} und Radius ${r}. Die Abzugsquadrate haben halbe Seitenlänge ${r/2}. Der lineare Ausgang darf negative Werte liefern.`],fields:[field('radius','Radius der Grundraute',r),field('square','Radius eines Abzugsquadrats',r/2),field('upper','Zentrum des oberen Abzugsquadrats',centers[1],{vector:true}),field('lower','Zentrum des unteren Abzugsquadrats',centers[2],{vector:true})],solution,checks:['Zentren, Metriken und Radien angegeben.','Hidden-Aktivierungen und linearer Ausgang vollständig definiert.','Gewünschtes Inneres, Abzüge und Außenbereich begründet.'],model:{centers,r,sgn,polys}}]};
 }
 function approximation(kind){const o=kind==='original',a=o?C.approximation([1,2,2],-4,4,1):C.approximation([1,-1,1],-2,2,.5),formula=o?'x^2+2x+2':'x^2-x+1';
  const rows=a.xs.map((x,i)=>[x,a.ys[i],i?a.deltas[i-1]:'Startwert']);
  const calculations=a.xs.map((x,i)=>detail(`Rechnung bei x = ${x}`,T`<p>$f(${x})=${a.coeff[0]}\cdot(${x})^2+(${a.coeff[1]})\cdot(${x})+${a.coeff[2]}=${a.ys[i]}$.</p>`+(i?T`<p>MLP-Gewicht: $${a.ys[i]}-${a.ys[i-1]}=${a.deltas[i-1]}$.</p>`:'<p>Dies ist der Startwert des MLP.</p>'))).join('');
  const common=table(['Stützstelle / Zentrum','f(xᵢ)','MLP-Stufengewicht'],rows);
  const mid=a.start+a.step/2;
  const mlpSolution=T`<h3>1. Stützwerte und Unterschiede</h3><p>Wir verwenden Abstand $h=${a.step}$ und neun Stützwerte. Acht Hidden-Neuronen schalten an den Stellen $x_1,\ldots,x_8$.</p>`+common+T`<p>Erster Unterschied: $${a.ys[1]}-${a.ys[0]}=${a.deltas[0]}$. Nicht die Höhe selbst als Stufengewicht verwenden.</p>`+detail('Alle Stützwerte einzeln nachrechnen',calculations)+T`<h3>2. Das gesamte Netz</h3><p>Ein Eingang $x$, acht Hidden-TLUs, ein linearer Ausgang: $1+8+1=10$ Neuronen, drei Schichten einschließlich Eingabe.</p>$$h_i(x)=\mathbf1_{x\ge x_i},\quad i=1,\ldots,8,$$$$\widehat f(x)=${a.ys[0]}+\sum_{i=1}^8\Delta y_i h_i(x).$$<p>Alle Hidden-Eingangsgewichte sind 1; Schwellen sind ${a.xs.slice(1).join(', ')}. Ausgabegewichte sind ${v(a.deltas)}. Ausgangsschwelle: $\theta=-${a.ys[0]}$. Ausgangsaktivierung: $net-\theta$; Ausgabefunktionen sind Identitäten.</p><h3>3. Kontrolle und Zeichnung</h3><p>Vor der ersten Schwelle ist die Ausgabe ${a.ys[0]}. Ab der ersten Schwelle ist sie $${a.ys[0]}+(${a.deltas[0]})=${a.ys[1]}$. Die Unterschiede summieren sich an jeder Stützstelle zum gewünschten Wert.</p>`+V.approx(a);
  const rbfSolution=T`<h3>1. Zentren und Gewichte</h3><p>Neun Zentren bei ${a.xs.join(', ')}, euklidischer Abstand in einer Dimension $d_i=|x-c_i|$, Radius $${a.step}$.</p>`+common+T`<h3>2. Dreiecksaktivierung und linearer Ausgang</h3>$$h_i(x)=\max\left(0,1-\frac{|x-c_i|}{${a.step}}\right),$$$$\widehat f(x)=\sum_{i=0}^8 f(c_i)h_i(x).$$<p>Ausgangsgewichte sind die Funktionswerte in der Tabelle. Ausgangsschwelle 0; lineare Aktivierung. Alle Ausgabefunktionen sind Identitäten. Ein Eingang + neun Hidden-Neuronen + ein Ausgang = 11 Neuronen.</p><h3>3. Zwischenpunkt vollständig rechnen</h3><p>Bei $x=${mid}$ sind die Abstände zu den Zentren $${a.xs[0]}$ und $${a.xs[1]}$ jeweils $${a.step/2}$. Beide Aktivierungen sind $1-${a.step/2}/${a.step}=0.5$.</p>$$\widehat f(${mid})=0.5\cdot${a.ys[0]}+0.5\cdot${a.ys[1]}=${a.rbf(mid)}.$$<p>Die echte Funktion hat dort Wert ${f(a.f(mid))}; eine Approximation muss zwischen den Stützstellen nicht exakt sein. An den Zentren ist jeweils genau eine Aktivierung 1. Dazwischen summieren sich zwei Aktivierungen zu 1. Es entsteht die eingezeichnete lineare Interpolation.</p>`+V.approx(a)+detail('Alle Funktionswerte nachrechnen',calculations);
  return {id:`${kind}-approx`,family:'approx',kind,title:'Eine Funktion mit MLP und RBF annähern',purpose:o?'Aufgabe 3 · alle drei Teilaufgaben':'Anderes Intervall und andere Schrittweite selbst wählen',source:'2024, Aufgabe 3 · Folien 80, 99–104, 291–298',parts:[
   {id:'mlp',title:'a · MLP',recipes:['mlp'],prompt:T`Approximiere $f(x)=${formula}$ auf $[${a.start},${a.end}]$ mit einem dreischichtigen Perzeptron mit insgesamt $8\le n\le12$ Neuronen. Gib die Parameter an und zeichne die Näherung.`,clarification:'Die Eingabeschicht zählt mit. Die Musterlösung ist eine mögliche Konstruktion; andere korrekte Näherungen sind zulässig.',hints:[`Wähle neun gleichmäßig verteilte Stützwerte zwischen ${a.start} und ${a.end}. Daraus entstehen acht Stufenneuronen.`,T`Berechne erst die Höhen. Die Gewichte sind $f(x_i)-f(x_{i-1})$.`,T`Der Startwert ist $f(${a.start})=${a.ys[0]}$. Bei $out=net-\theta$ muss die Ausgangsschwelle negativ sein.`],fields:[field('start',T`Startwert f(${a.start})`,a.ys[0]),field('delta',`Erstes Stufengewicht bei Schrittweite ${a.step}`,a.deltas[0]),field('theta','Ausgangsschwelle dieser Stufenkonstruktion',-a.ys[0])],solution:mlpSolution+detail('Netzzeichnung mit Ausgabegewichten',V.network(a.deltas.map((_,i)=>'h'+(i+1)),a.deltas,'MLP mit einem Eingang, acht Hidden-TLUs und linearem Ausgang',1)),checks:['Stützwerte und Stufenunterschiede stimmen.','Alle Schwellen, Gewichte, Aktivierungen und der Startwert angegeben.','Neuronenzahl eingehalten und Graph gezeichnet.'],model:{coeff:a.coeff,start:a.start,end:a.end,step:a.step}},
   {id:'rbf',title:'b · RBF',recipes:['approx'],prompt:T`Approximiere dieselbe Funktion $f(x)=${formula}$ auf $[${a.start},${a.end}]$ mit einem RBF-Netz mit $8\le n\le12$ Neuronen. Gib alle Parameter an und zeichne die Näherung.`,hints:['Wähle neun gleichmäßig verteilte Zentren. Dreiecksfunktionen erlauben eine stetige lineare Interpolation.',`Der Radius entspricht dem Abstand benachbarter Zentren: ${a.step}. Ausgabegewichte sind die Funktionswerte.`,T`Am Mittelpunkt zwischen zwei Zentren tragen beide mit $0.5$ bei.`],fields:[field('radius','Radius bei neun gleichmäßig verteilten Zentren',a.step),field('middle',`Ausgabe der Dreieckskonstruktion bei x = ${mid}`,a.rbf(mid)),field('nodes','Gesamte Neuronenzahl dieser Konstruktion',11,{tolerance:0})],solution:rbfSolution+detail('Netzzeichnung mit Ausgabegewichten',V.network(a.ys.map((_,i)=>'h'+i),a.ys,'RBF-Netz mit einem Eingang, neun Basisfunktionen und linearem Ausgang',1)),checks:['Zentren, Distanzen, Radien, Aktivierungen und Ausgabegewichte angegeben.','Ein Zwischenpunkt und die Stützstellen kontrolliert.','Neuronenzahl und Zeichnung vollständig.']},
   {id:'verbessern',title:'c · Verbessern',recipes:['mlp','approx'],prompt:'Nenne mindestens zwei Möglichkeiten, die Approximationen zu verbessern.',hints:['Unterscheide eine feinere Unterteilung und eine bessere Konstruktion bei gleicher Knotenzahl.','Ein bloßer Aktivierungstausch lässt die alten Gewichte nicht automatisch sinnvoll bleiben.'],fields:[],solution:T`<p><strong>1. Mehr Stützstellen und Neuronen</strong>, sofern für diesen Verbesserungsvorschlag die feste Knotengrenze aufgehoben wird. Kleinere Intervalle verringern bei geeigneter Konstruktion den Näherungsfehler.</p><p><strong>2. Bei gleicher Knotenzahl geeignete stetige Basisfunktionen verwenden und die Parameter neu bestimmen.</strong> Beispielsweise die Stufennäherung durch stückweise lineare Näherung ersetzen. Das muss zur Architektur passen.</p><p><strong>3. Zentren, Breiten und Gewichte anhand des Approximationsfehlers optimieren.</strong> Den Fehler anschließend prüfen; ein Trainingsverfahren garantiert nicht automatisch das globale Optimum.</p><p>Für die Aufgabe genügen zwei klar erläuterte Möglichkeiten. Schreibe eine notwendige Bedingung wie „wenn mehr Neuronen erlaubt sind“ dazu.</p>`,checks:['Mindestens zwei konkrete Änderungen genannt.','Neuronengrenze und notwendige Anpassung der Gewichte berücksichtigt.']}
  ]};
 }
 function lvqTable(data,eta){return table(['Punkt','d² zu A / B','Winner','A danach','B danach'],data.rows.map(r=>[r.point.name,v(r.ds),r.win?'B':'A',v(r.centers[0].v),v(r.centers[1].v)]))+data.rows.map(r=>detail(`Rechnung für ${r.point.name} = ${v(r.point.v)}`,T`<p>Alte Prototypen: A=${v(r.old[0].v)}, B=${v(r.old[1].v)}. Abstandsquadrate:</p>`+r.old.map((p,i)=>T`$$d^2(${r.point.name},${i?'B':'A'})=(${r.point.v[0]}-${p.v[0]})^2+(${r.point.v[1]}-${p.v[1]})^2=${r.ds[i]}.$$`).join('')+r.old.map((p,i)=>r.signs[i]?T`$$${i?'B':'A'}^{neu}=${v(p.v)}${r.signs[i]>0?'+':'-'}${eta}\,[${v(r.point.v)}-${v(p.v)}]=${v(r.centers[i].v)}.$$`:`<p>${i?'B':'A'} bleibt ${v(p.v)}.</p>`).join(''))).join('')+table(['Prototyp','Endwert','Gesamtänderung'],data.centers.map((r,i)=>[i?'B':'A',v(r.v),v(data.changes[i])]));}
 function somSolution(grid,x,eta,sigma){const data=C.som(grid,x,eta,sigma),win=grid[data.winner];return {data,html:T`<h3>1. Winner im Datenraum</h3><p>Der nächste Prototyp ist $${v(win.v)}$, Gitterindex $${v(win.g)}$. Im rechteckigen Anfangsgitter wähle je Koordinate den nächsten vorhandenen Wert. Für $x=${v(x)}$ sind das ${win.v[0]} und ${win.v[1]}. Der Winner-Abstand ist $(${x[0]}-${win.v[0]})^2+(${x[1]}-${win.v[1]})^2=${C.dist(win.v,x)}$.</p><h3>2. Faktoren im Gitter</h3>$$d_G^2=(i-${win.g[0]})^2+(j-${win.g[1]})^2,\quad h=e^{-d_G^2/(2\cdot${sigma}^2)},$$$$\Delta r=${eta}\,h\,(${v(x)}-r),\quad r^{neu}=r+\Delta r.$$<p>Alle neuen Vektoren verwenden die alten Positionen. Jede Zeile unten enthält ihre eigene vollständige Rechnung. Gleiche Gitterabstände dürfen denselben Faktor wiederverwenden.</p>`+table(['Gitterindex','Alter Vektor','d² im Gitter','h','Änderung Δr','Neuer Vektor'],data.rows.map(r=>[v(r.g),v(r.v),r.d2,f(r.h),v(r.delta),v(r.next)]))+detail('Jede Zeile vollständig nachrechnen',data.rows.map(r=>detail(`Neuron ${v(r.g)} · alter Vektor ${v(r.v)}`,T`$$d_G^2=(${r.g[0]}-${win.g[0]})^2+(${r.g[1]}-${win.g[1]})^2=${r.d2},$$$$\alpha=${eta}e^{-${r.d2}/(2\cdot${sigma}^2)}\approx${f(r.alpha)},$$$$\Delta r=\alpha\,${v(x.map((z,i)=>z-r.v[i]))}\approx${v(r.delta)},$$$$r^{neu}=${v(r.v)}+\Delta r\approx${v(r.next)}.$$`)).join(''))+V.plane({range:[-1,Math.max(...grid.map(r=>r.v[0]))+2,-1,Math.max(...grid.map(r=>r.v[1]))+2],lines:data.rows.map(r=>[r.v,r.next]),points:[{x:x[0],y:x[1],label:'Datenpunkt'},...data.rows.map(r=>({x:r.next[0],y:r.next[1],label:''}))],title:'Verschiebungen vom alten zum neuen Prototyp'})+`<p>Die Strecken zeigen die Verschiebungen; Punkte markieren die neuen Prototypen. Bei diesen Lernraten liegt jede neue Koordinate zwischen alter Koordinate und Datenkoordinate. Der Gaußfaktor wird auch außerhalb der Breite σ nicht auf null gesetzt.</p>`};}
 function learning(kind){const o=kind==='original',initial=o?[{v:[5,4],label:'A'},{v:[7,5],label:'B'}]:[{v:[2,2],label:'A'},{v:[6,2],label:'B'}],points=o?[{name:'p',v:[1,6],label:'A'},{name:'q',v:[9,1],label:'B'}]:[{name:'p',v:[3,2],label:'B'},{name:'q',v:[4,4],label:'A'}],eta=.5,plain=C.lvq(initial,points,eta,'none'),supervised=C.lvq(initial,points,eta,'winner'),both=C.lvq(initial,points,eta,'two'),grid=[];
  for(let j=0;j<=(o?6:2);j++)for(let i=0;i<=(o?8:2);i++)grid.push({g:[i,j],v:[5*i,5*j]});const x=o?[28,20]:[8,6],somEta=o?.5:.4,sigma=o?2:1,som=somSolution(grid,x,somEta,sigma),neighbor=som.data.rows.find(r=>r.d2===2);
  const given=`Start: A=${v(initial[0].v)} gehört zur weißen Klasse A; B=${v(initial[1].v)} zur schwarzen Klasse B. Datenreihenfolge: ${points.map(p=>`${p.name}=${v(p.v)} aus Klasse ${p.label}`).join(', dann ')}. Lernrate η=0.5.`;
  return {id:`${kind}-learning`,family:'learning',kind,title:'LVQ und SOM: Vektoren verändern',purpose:o?'Aufgabe 4 · eine Epoche und das SOM-Gitter':'Falsche Gewinnerklasse und veränderte Winner-Wahl; kleineres SOM-Gitter',source:'2024, Aufgabe 4 · Folien 336, 340, 364–367',parts:[
   {id:'ohne',title:'a I · Ohne Klassen',recipes:['lvq'],prompt:given+' Berechne eine Epoche ohne Berücksichtigung der Klassen. Gib die Gesamtänderungen der Codebook-Vektoren an.',image:o?'exam_task_img_25.png':null,hints:['Berechne die zwei Abstandsquadrate zu p. Ohne Klassen wird nur der nächste Prototyp angezogen.','Für q müssen die bereits nach p veränderten Vektoren verwendet werden.'],fields:[field('A','Endvektor A',plain.centers[0].v,{vector:true}),field('B','Endvektor B',plain.centers[1].v,{vector:true})],solution:`<p>${given}</p><h3>Nur der Winner lernt</h3>`+lvqTable(plain,eta)+T`<p>Formel: $r'=r+0.5(x-r)$. In jeder Zeile bleibt der Nichtgewinner unverändert. Die Gesamtänderung bezieht sich auf den ursprünglichen Start, nicht nur auf den letzten Schritt.</p>`,checks:['Winner für jeden Punkt neu bestimmt.','Nur Winner verändert; beim zweiten Punkt aktuelle Vektoren verwendet.','Endwerte und Gesamtänderungen angegeben.'],model:{initial,points,eta,rule:'none'}},
   {id:'mit',title:'a II · Mit Klassen',recipes:['lvq'],prompt:given+' Berechne eine Epoche mit Berücksichtigung der Klassen. Beginne erneut mit den ursprünglichen Prototypen.',clarification:o?'Die Vorlage nennt keine eindeutige LVQ-Variante. Hier ist die Hauptrechnung die Gewinnerregel aus Folie 336. Die vollständige alternative Rechnung nach Folie 340 steht getrennt in der Lösung.':'Für diese Transferaufgabe gilt ausdrücklich die Gewinnerregel aus Folie 336: nur den nächsten Prototyp verändern, bei falscher Klasse abstoßen.',hints:['Prüfe erst den Abstand, dann die Klasse des Winners. Ein falscher Winner wird abgestoßen.',o?'Hier passt die Gewinnerklasse bei beiden Punkten. Die Gewinnerregel liefert deshalb dieselben Endwerte wie ohne Klassen.':'Nach dem Abstoßen bei p kann sich die Winner-Wahl für q ändern. Rechne die Abstände neu.'],fields:[field('A','Endvektor A nach Gewinnerregel',supervised.centers[0].v,{vector:true}),field('B','Endvektor B nach Gewinnerregel',supervised.centers[1].v,{vector:true})],solution:`<h3>Gewinnerregel aus Folie 336</h3><p>${given}</p>`+lvqTable(supervised,eta)+detail('Andere Regel: vollständige Zweiprototypen-Rechnung (Folie 340)',`<p>Dies ist eine getrennte Lesart mit erneutem Start. Betrachte die zwei nächsten Prototypen: verschiedene Klassen, und genau eine gehört zur Datenklasse. Dann richtigen anziehen, falschen abstoßen. Hier gibt es genau einen Prototyp je Klasse. Keine zusätzliche Window-Regel ist angegeben.</p>`+lvqTable(both,eta))+`<p>Schreibe vor einer Rechnung die verwendete Regel auf. „Klassen berücksichtigen“ allein unterscheidet die beiden Varianten nicht sicher. Für dieses Training werden die Eingabefelder anhand der ausdrücklich genannten Gewinnerregel geprüft.</p>`,checks:['Verwendete Regel klar benannt.','Vorzeichen anhand der Gewinnerklasse gewählt.','Für diesen Durchlauf neu gestartet und die Reihenfolge eingehalten.'],model:{initial,points,eta,rule:'winner'}},
   {id:'som',title:'b · SOM',recipes:['som'],prompt:T`Berechne die Änderungen der Codebook-Vektoren für $x=${v(x)}$, Lernrate $\eta=${somEta}$, euklidische Distanz und Gauß-Nachbarschaft mit Breite $\sigma=${sigma}$.`,clarification:o?T`Lesart der Abbildung: Jede Gitterkreuzung ist anfangs ein Prototyp $(5i,5j)$, $i=0,\ldots,8$, $j=0,\ldots,6$. Ein direkter Gitterschritt hat Abstand 1. Die Vorlage markiert die Neuronen nicht einzeln; diese Annahme wird für die vollständige Rechnung offengelegt.`:T`Neun Neuronen: Gitterindizes $i,j\in\{0,1,2\}$, Anfangsprototypen $(5i,5j)$. Ein direkter Gitterschritt hat Abstand 1. Bestimme den Winner selbst.`,image:o?'exam_task_img_28.png':null,figure:o?'':V.plane({range:[-1,12,-1,12],points:grid.map(r=>({x:r.v[0],y:r.v[1],label:v(r.g)})).concat([{x:x[0],y:x[1],label:'x'}]),title:'Anfangsprototypen mit Gitterindizes und Datenpunkt'}),hints:['Winner: Vergleiche Abstände zum Datenpunkt im Datenraum.','Berechne danach Abstände der Gitterindizes zum Winner. Ein diagonaler Nachbar hat d²=2.',T`Beim Winner ist $h=1$. Für jedes andere Neuron bleibt $h=e^{-d_G^2/(2\sigma^2)}$ positiv.`],fields:[field('winner','Alter Winner-Vektor',grid[som.data.winner].v,{vector:true}),field('new','Neuer Winner-Vektor',som.data.rows[som.data.winner].next,{vector:true}),field('diag',`Neuer Vektor des Neurons mit Gitterindex ${v(neighbor.g)}`,neighbor.next,{vector:true})],solution:som.html,checks:['Winner und Gitterindex bestimmt.','Gitterabstand und Datenraum getrennt.','Alle verlangten Änderungen mit derselben alten Gitterlage berechnet.'],model:{grid,x,eta:somEta,sigma}}
  ]};
 }
 function hopfield(kind){const o=kind==='original',W=o?[[0,-2,0],[-2,0,-2],[0,-2,0]]:[[0,1,-1],[1,0,2],[-1,2,0]],theta=o?[-1,-2,-1]:[0,1,-1],rows=C.hopfield(W,theta),sign=V.sign;
  const tab=table(['Ausgang','net₁, net₂, net₃','nur u₁','nur u₂','nur u₃','E','stabil'],rows.map(r=>[sign(r.s),v(r.net),...r.next.map(sign),r.energy,r.stable?'ja':'nein']));
  const calculations=rows.map(r=>detail(`Alle Updates aus ${sign(r.s)}`,W.map((w,i)=>T`$$net_${i+1}=${w.map((z,j)=>`${z}\cdot(${r.s[j]})`).join('+')}=${r.net[i]}\ ${r.net[i]>=theta[i]?'\ge':'<'}\ ${theta[i]}.$$<p>Nur $u_${i+1}$ aktualisieren: ${sign(r.s)} → ${sign(r.next[i])}. Die anderen beiden Zustandswerte bleiben erhalten.</p>`).join(''))).join('');
  return {id:`${kind}-hopfield`,family:'hopfield',kind,title:'Hopfield: alle erlaubten Übergänge',purpose:o?'Aufgabe 5 · Originalnetz':'Andere Verbindungen, Schwellen und Gleichheitsfälle',source:'2024, Aufgabe 5 · Folien 380–390',parts:[{id:'graph',title:'Zustandsgraph',recipes:['hopfield'],prompt:o?'Zeichne den vollständigen Zustandsgraphen des abgebildeten Hopfield-Netzes. Es ist keine Update-Reihenfolge festgelegt. Berücksichtige alle möglichen Nachfolgezustände.':T`Drei bipolare Neuronen mit symmetrischen Gewichten $w_{12}=1$, $w_{13}=-1$, $w_{23}=2$, Diagonale null und Schwellen $\theta=(0,1,-1)$. Zeichne den vollständigen Graphen aller möglichen asynchronen Einzelupdates.`,clarification:T`Zustandsreihenfolge $(u_1,u_2,u_3)$; bipolar. Schwellenregel: neuer Wert $+1$ bei $net_i\ge\theta_i$, sonst $-1$. ${o?'Die Kreiswerte werden als Schwellen gelesen: θ=(−1,−2,−1).':''}`,image:o?'exam_task_img_29.png':null,hints:['Liste zuerst die acht Zustände. Pro Zeile brauchst du drei getrennte Nachfolger.',`Aus −−− sind die Netzeingaben ${v(rows[0].net)}. Vergleiche jeden Wert mit seiner eigenen Schwelle.`,T`Bei Gleichheit gilt die $\ge$-Regel. Prüfe jeden Nachfolger vom ursprünglichen Zeilenzustand aus.`],fields:[field('net','Netzeingaben bei −−−',rows[0].net,{vector:true}),field('next','Nachfolger von −−− bei Update nur von u₂ (drei Zahlen)',rows[0].next[1],{vector:true}),field('stable','Anzahl stabiler Zustände',rows.filter(r=>r.stable).length,{tolerance:0})],solution:T`<h3>1. Netzeingaben</h3>$$net_1=${W[0][1]}s_2+(${W[0][2]})s_3,$$$$net_2=${W[1][0]}s_1+(${W[1][2]})s_3,$$$$net_3=${W[2][0]}s_1+(${W[2][1]})s_2.$$<p>Schwellen sind ${v(theta)}. Bei Ausgang −−− ergibt das ${v(rows[0].net)}. Für jedes Einzelupdate bleiben die anderen beiden Zeichen unverändert.</p><h3>2. Vollständige Tabelle</h3>`+tab+detail('Jede Zeile vollständig nachrechnen',calculations)+`<h3>3. Vollständiger Graph</h3>`+V.graph(rows)+`<p><strong>Unveränderte Updates:</strong> ${rows.map(r=>{const same=r.next.flatMap((n,i)=>n.every((x,j)=>x===r.s[j])?['u'+(i+1)]:[]);return same.length?`${sign(r.s)}: ${same.join(', ')}`:'';}).filter(Boolean).join(' · ')}.</p><p>Insgesamt acht Zustände × drei Einzelupdates = 24 Tabellenzellen. Stabile Zustände: ${rows.filter(r=>r.stable).map(r=>sign(r.s)).join(', ')}.</p>`+detail('Zusätzliche Energiekontrolle',T`$$E=-\tfrac12s^\top Ws+\theta^\top s,\qquad \Delta E=-(s_i'-s_i)(net_i-\theta_i)\le0.$$<p>Bei Gleichheit von Netzeingabe und Schwelle kann −1 zu +1 wechseln, ohne dass die Energie fällt. Die Energie ist eine Kontrolle; die Vorlage verlangt vor allem den vollständigen Zustandsgraphen.</p>`),checks:['Alle acht Ausgangszustände und 24 Einzelupdates erfasst.','Pro Übergang höchstens ein Neuron verändert.','Alle echten Pfeile und unveränderten Updates angegeben.','Stabile Zustände korrekt markiert.'],model:{W,theta}}]};
 }
 const tasks=['original','transfer'].flatMap(kind=>[tlu(kind),rbf(kind),approximation(kind),learning(kind),hopfield(kind)]);
 const repairData=[
  ['tlu','Seite einer Geraden',T`Die Randgerade ist $x_1+x_2=4$. Die gewünschte Seite enthält $(1,1)$. Gib $(w_1;w_2;\theta)$ einer passenden TLU an.`,field('side','Gewichte und Schwelle',[-1,-1,-4],{vector:true,scale:true}),T`<p>Test mit $(1,1)$: $1+1=2\le4$. Also $-x_1-x_2\ge-4$. Gewichte $(-1,-1)$, Schwelle $-4$. Positive gemeinsame Skalierungen sind gleichwertig.</p>`,'tlu'],
  ['rbf','Radius eines Quadrats',T`Ein Quadrat reicht von $x_1=2$ bis $6$ und von $x_2=1$ bis $5$. Welchen Radius hat der $L_\infty$-Ball um seinen Mittelpunkt?`,field('radius','Radius',2),T`<p>Mittelpunkt $(4,3)$. Jede Koordinate darf um $2$ abweichen. Radius $2$, Seitenlänge $4$.</p>`,'rbf'],
  ['approx','Höhe oder Stufenunterschied?',T`Ein MLP soll nacheinander die Höhen $7,4,6$ annehmen. Welches Ausgabegewicht gehört zum zweiten Sprung (von 4 auf 6)?`,field('delta','Ausgabegewicht',2),T`<p>$\Delta y=6-4=2$. Kontrolle: Start $7$, erster Sprung $-3$, zweiter Sprung $+2$: $7-3+2=6$.</p>`,'mlp'],
  ['learning','Nach dem ersten Update weiterrechnen',T`Nach dem ersten Datenpunkt steht der Winner bei $r=(3,5)$. Der nächste Punkt ist $q=(9,1)$, Lernrate $0.5$. Dieser Prototyp soll angezogen werden. Berechne seinen neuen Vektor.`,field('update','Neuer Vektor',[6,3],{vector:true}),T`<p>$r'= (3,5)+0.5[(9,1)-(3,5)]=(3,5)+(3,-2)=(6,3)$. Verwende den Stand nach dem ersten Update.</p>`,'lvq'],
  ['hopfield','Nur ein Neuron aktualisieren',T`Ausgang $s=(1,-1,-1)$. Für $u_2$ gilt $net_2=0$, $\theta_2=-2$. Berechne den Zustand nach genau diesem Einzelupdate.`,field('state','Neuer Zustand',[1,1,-1],{vector:true}),T`<p>$0\ge-2$, also wird $s_2=+1$. Die übrigen Werte bleiben gleich: $(1,1,-1)$.</p>`,'hopfield'],
  ['learning','Diagonal im Neuronengitter',T`Winner-Index $(2,1)$, Nachbar-Index $(1,2)$. Bestimme das quadrierte Gittermaß $d_G^2$.`,field('grid','d² im Gitter',2),T`<p>$d_G^2=(1-2)^2+(2-1)^2=1+1=2$. Dafür werden keine Datenraumkoordinaten benötigt.</p>`,'som']
 ];
 repairData.forEach(([family,title,prompt,q,solution,recipe],i)=>tasks.push({id:`repair-${i}`,family,kind:'repair',title,purpose:'Kurze Teilübung',source:recipes[recipe].source,parts:[{id:'schritt',title:'Ein Schritt',recipes:[recipe],prompt,hints:[recipes[recipe].example],fields:[q],solution,checks:['Rechnung ohne Vorlage nachvollzogen.']}]}));
 const families=[['tlu','1 · TLU-Fläche'],['rbf','2 · RBF-Fläche'],['approx','3 · Funktionsapproximation'],['learning','4 · LVQ und SOM'],['hopfield','5 · Hopfield']];
 root.NNContent={tasks,recipes,recipeHTML,table,detail,families};
 if(typeof module!=='undefined'&&module.exports)module.exports=root.NNContent;
})(typeof globalThis!=='undefined'?globalThis:this);
